Warm editorial surface card — hairline border + soft wine-tinted shadow.

```jsx
<Card surface="paper" elevation="card" padding={28}>…</Card>
<Card surface="wine">Reversed content</Card>
<Card surface="sand" elevation="sm" radius="lg">…</Card>
```

`surface`: paper / cream / sand / blush / plum / wine / charcoal. `elevation`: none / sm / card / float / lg. `radius`, `padding`, `border`.
