The Big 3 triangle icon mark (Moon · Sun · Ascendant). Renders the official asset.

```jsx
<TrinityMark size={64} base="../assets" />
<TrinityMark size={40} tone="white" base="../../assets" />
```

Pass `base` = relative path to the /assets folder from your HTML. `tone`: color / white / ink.
