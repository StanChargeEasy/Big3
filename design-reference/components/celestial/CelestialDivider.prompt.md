Thin editorial rule flanked by luminary glyphs — the brand's section separator.

```jsx
<CelestialDivider variant="duo" base="../assets" />
<CelestialDivider variant="trinity" color="gold" base="../../assets" />
```

`variant`: duo (Moon—Sun) / trinity (Moon—ASC—Sun). Pass `base` = path to /assets.
