Full horizontal Big 3 Astrology wordmark lockup.

```jsx
<Logo variant="colorDark" height={56} base="../assets" />
<Logo variant="colorLight" height={40} base="../../assets" />  /* on dark bg */
```

`variant`: colorDark / colorLight / black / white. Pass `base` = path to /assets.
